import { LoaderContainer, StyledLoader } from "./styles";

const Loader = () => {
   return (
      <LoaderContainer>
         <StyledLoader />
      </LoaderContainer>
   );
};

export default Loader;
