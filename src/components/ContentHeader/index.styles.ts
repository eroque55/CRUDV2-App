import styled from "styled-components";

export const StyledContentHeader = styled.header`
   display: flex;
   flex-direction: column;
   gap: 1rem;
`;

export const StyledContentHeaderOptions = styled.div`
   display: flex;
   gap: 1.25rem;
   align-items: center;
   justify-content: flex-end;
`;
