import styled from "styled-components";

export const StyledTableBody = styled.p`
   color: ${({ theme }) => theme.colors.neutros.cor7};
   font-size: 1rem;
   font-weight: 400;
   width: 100%;
   text-align: left;
   cursor: pointer;
`;
